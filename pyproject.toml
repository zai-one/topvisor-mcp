[build-system]
requires = ["hatchling==1.28.0"]
build-backend = "hatchling.build"

[project]
name = "zai-topvisor-mcp"
version = "0.3.0"
description = "Compare saved SEO rankings, organise keywords and control paid Topvisor checks from your assistant"
readme = "README.md"
license = "LicenseRef-ZAI-ONE"
license-files = ["LICENSE", "NOTICE"]
requires-python = ">=3.12,<3.15"
dependencies = [
  "fastmcp==3.4.4",
  "httpx==0.28.1",
]

[dependency-groups]
dev = ["pytest==9.1.1", "pytest-asyncio==1.4.0", "ruff==0.15.21"]

[project.scripts]
topvisor-check-reconcile = "zai_topvisor.rank_checks:reconcile_main"
topvisor-mcp-setup = "zai_topvisor.setup:main"
topvisor-mcp = "zai_topvisor.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/zai_topvisor"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]

[project.urls]
Homepage = "https://github.com/zai-one/topvisor-mcp"
Issues = "https://github.com/zai-one/topvisor-mcp/issues"

[tool.hatch.build.targets.sdist]
include = ["src", "tests", "scripts", "contracts", "examples", "docs", "pyproject.toml", "uv.lock", "README.md", "README.ru.md", "INSTALL.md", "LICENSE", "NOTICE", "CHANGELOG.md", "CONTRIBUTING.md", "SECURITY.md", "mcp.example.json", ".env.example", "SOURCE_PROVENANCE.json", "third_party", "vendor"]
